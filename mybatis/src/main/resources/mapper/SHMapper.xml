<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mybatis.dao.SHDao">
    <select id="findAll" resultType="com.example.mybatis.entity.stuhom">select * from stuhom</select>
    <insert id="addStuHom" parameterType="stuhom">insert into s_h(id,s_id,h_id,h_title,h_content,s_content,time) values(?,?,?,?,?,?,?)</insert>
</mapper>